// ------------------------------------------------------------------
// Immagini reali del progetto (opzionali).
// Metti i file in src/assets/ e importali qui, oppure incolla un URL
// o un data-URI. Se un valore è null, il componente usa il fallback
// grafico (SVG) già pronto, senza rompere il layout.
//
// Esempio:
//   import yarn from "../assets/gomitoli.jpg";
//   export const YARN_PHOTO = yarn;
// ------------------------------------------------------------------

export const YARN_PHOTO = null;   // gomitoli colorati (hero, a destra)
export const FRUIT_PHOTO = null;  // agrumi/frutta (sfondo Index e Ricerca)
